# Normalise line endings so Windows and CI checkouts do not produce
# whole-file diffs. Batch files must stay CRLF to run correctly.
* text=auto eol=lf

*.bat text eol=crlf

*.png binary
*.jpg binary
*.webp binary
*.svg text eol=lf
